# Gradle
.gradle/
**/build/

# Android
local.properties
*.apk
*.aab
*.ap_
*.dex
captures/

# IDE
.idea/
*.iml
*.ipr
*.iws
.DS_Store

# Kotlin / JVM
*.hprof

# Orchestrator scratch (intermediate artifacts, not project deliverables)
_workspace/

# MCP / tooling caches
.serena/

# Claude Code native worktrees (isolated per-slice workspaces)
.claude/worktrees/

# MediaPipe model bundle (large binary; downloaded at build time)
app/src/main/assets/*.task

# On-device form-classifier models (large binaries; dropped by the ml track, see assets/models/README)
app/src/main/assets/models/*.tflite

.claude/mcp/.venv/
.claude/scheduled_tasks.lock

# Local CPU dev/test environment (no GPU, no mediapipe, no tensorflow).
# Install:  uv venv --python 3.11 && uv pip install -r requirements-dev.txt
numpy>=1.26
scikit-learn>=1.4
pandas>=2.0
kagglehub[pandas-datasets]>=1.0
joblib>=1.3
PyYAML>=6.0
pytest>=8.0
